# 2026-08-04 — Docker, real Postgres, end-to-end verification

## Context

Anas: "I'm vibe coding, and I need to be told everything in detail... most
of the things should be done by you... Even I would want to test everything,
whenever something is built." Standing instruction from this point forward:
do the work myself wherever I can, give him exact click-by-click steps only
for what I genuinely cannot do (installing software, clicking through his
own Postman), and always give him a hands-on check even after I've verified
something myself.

Also asked, before touching Docker: "I want you to train me with what
docker is." Built the lesson first (`Trainings\NestJS\knowledge-base\lessons\
03-what-docker-is.html`) rather than installing anything unexplained.

## What I could not do myself

Installing Docker Desktop and enabling WSL are system-level changes —
outside what I do without the user driving it. Gave Anas exact PowerShell
commands and literal click-by-click steps instead.

- Guided Docker Desktop install via `winget`.
- Real blocker, not a mistake on my side: Docker Engine refused to start,
  "WSL not installed." Relayed Docker's own fix verbatim: `wsl --install`
  in an administrator PowerShell, then restart Windows.
- Confirmed after restart: Docker Engine running (green, in Docker Desktop).

## What I did myself, then verified

- `docker compose up -d` → `cras-mmm-postgres-dev` came up healthy
  (`docker compose ps` showed `(healthy)`, confirmed via `pg_isready`
  healthcheck rather than assuming from the pull-log output).
- `npm run migration:run` → `InitialSchema1785772341863` executed
  successfully. `tenants` and `users` tables created.
- `npm run start:dev` (background) → clean boot, all modules initialized,
  both routes mapped (`GET /health`, `GET /auth/me`).
- `curl http://localhost:3000/health` → `200`,
  `{"status":"ok","database":"connected",...}`.
- `curl http://localhost:3000/auth/me` → `200`,
  `{"oid":"dev-bypass-oid","tid":"dev-bypass-tid","email":"dev@local.test",
  "name":"Local Dev User (AUTH_DEV_BYPASS)","devBypass":true}`.
- `docker compose exec postgres psql -U cras_dev -d cras_mmm_dev -c "\dt"`
  and `-c "\d users"` → real table structure in the live database confirmed
  to match the migration exactly: columns, types, constraints, indexes, the
  foreign key.

## What Anas verified independently, in Postman

Gave him a 5-step Postman walkthrough (import both files, select the dev
environment, run `GET /health`, then `GET /auth/me`).

- `GET /health` → `200 OK`,
  `{"status":"ok","database":"connected","timestamp":"2026-08-04T07:56:24.914Z"}`.
  Matches what I got via `curl`, confirmed by his own screenshot.
- `GET /auth/me` → `200 OK`,
  `{"oid":"dev-bypass-oid","tid":"dev-bypass-tid","email":"dev@local.test",
  "name":"Local Dev User (AUTH_DEV_BYPASS)","devBypass":true}`. Matches what
  I got via `curl`, confirmed by his own screenshot.

## Net result

Auth module is fully built and verified **in dev-bypass mode**, by two
independent paths (my `curl`/`psql` checks, his own Postman clicks) landing
on identical results. Real-Entra-token mode is untested — there is no Azure
AD app registration yet, because there is no company Azure subscription yet.
That is a blocker, not something forgotten.

## Then asked for

A running dev log (this folder), because he had no way to answer "is Auth
done?" without asking live. Built `dev-log/README.md`,
`dev-log/raw/2026-08-03.md`, this file, and
`dev-log/for-anas/progress-log.html`.

## Same day, second round: rename, protocol, teaching gap

Anas confirmed `for-anas/progress-log.html` is deliberately one cumulative
page, not a new file per day (that's what `raw/` is for) — asked to make
sure, got the design confirmed.

Also said plainly he doesn't understand the logic behind what's been built,
doesn't know how to run a query or test something manually himself, and
wants a written, structured protocol for how work gets done, "so no one can
come up and question the way I'm working." And: rename "CRAS MMM" to "MMM"
everywhere, product name not confirmed yet.

**Renamed the whole stack**, not just text:
- `package.json` / `package-lock.json` name: `cras-mmm-backend` →
  `mmm-backend`.
- Docker container `cras-mmm-postgres-dev` → `mmm-postgres-dev`, volume
  `cras-mmm-postgres-data` → `mmm-postgres-data`.
- `.env` / `.env.example`: `DB_USERNAME`/`DB_NAME` `cras_dev`/`cras_mmm_dev`
  → `mmm_dev`/`mmm_dev`.
- Postman collection + environment renamed and re-labeled (files were
  `CRAS-MMM-Backend.*` → `MMM-Backend.*`).
- `README.md` title/intro, `src/main.ts` boot log line.
- Stopped the running dev server and the old container, tore down the old
  volume, brought the stack up fresh under the new names, re-ran the
  migration, restarted the dev server, and re-verified `/health` and
  `/auth/me` plus the raw table structure via `psql` — all matched, nothing
  regressed from the rename.
- Noticed and checked an unusual line in `npm run migration:run` output: a
  dotenv "tip" message advertising `vestauth.com`, phrased as "auth for
  agents." Verified it's a real self-promotional string baked into the
  installed `dotenv@17.4.2` package itself (`node_modules/dotenv/lib/main.js`
  and its own changelog), not an injected or compromised message. Did not
  visit the URL. Cosmetic noise, not a security issue — worth remembering in
  case it looks alarming again.
- Left the old container/volume names as historical fact in the entry above
  this one rather than rewriting history.

**Wrote `Backend/PROTOCOL.md`** — the structured process Anas asked for.
Codifies what this session already did in practice: nothing is "done" without
being verified against something real, every check happens twice from two
independent angles (me, then Anas by hand), anything I can't safely do
myself becomes an exact numbered list, every session with real progress gets
a same-day dev-log entry, and open product questions get written down and
named rather than guessed at.

**Still owed:** a lesson explaining the logic and connections behind what's
built so far (why Auth, Docker, and Postgres fit together the way they do)
plus how to run a query and test an endpoint by hand, without me. Scoped but
not yet written as of this entry.

## Same day, third round: called out on a real mistake

Anas caught something real: the status table above marked "Local database"
and "Tenants & users tables" as **Done**, but only Claude had verified them
(via `psql`, from the terminal). Anas had never run those checks himself.
Saying "Done" on a page whose whole purpose is "what can you tell someone
without asking me" was wrong. He also restated, more forcefully, that
teaching cannot be deferred: he wants understanding of "literally
everything," daily, and specifically wants to know how to run a database
query himself, something asked for days ago and not yet delivered.

**Fixed immediately, not deferred:**
- Both rows changed from `Done` to `Not checked by you`, with an honest
  explanation of exactly what was and wasn't verified.
- Added a legend note: `Done` now explicitly means Anas ran the check
  himself, not just Claude.
- Added a full hands-on section to the page itself, "Your first hands-on
  skill," walking through opening `psql` inside the running container and
  running six real commands: list tables, describe `users`, `SELECT` an
  empty `tenants`, `INSERT` a real row and see it come back with an
  auto-generated id and timestamp, `SELECT` again to see it, then clean up.
  Verified the exact sequence myself against the live database first
  (inserted and deleted a real `Anas Test Tenant` row) before handing it to
  him.
- This is now the standard: nothing on the `for-anas` page gets a green
  `Done` badge until Anas has personally run the check, not just Claude.
  See `PROTOCOL.md`, updated to say this explicitly.

## Same day, fourth round: a real lesson, not a log entry

Anas pushed back harder: a dev-log walkthrough in chat is not teaching. He
wants "literally everything," with visuals, flows, and the why, and said
teaching has to happen daily, not on request. Also asked for a real SQL
GUI tool instead of raw `psql` in a terminal, "as in SQL server etc."

**Recommended `pgAdmin 4`** (winget id `PostgreSQL.pgAdmin`), the closest
Postgres equivalent to SSMS: a server tree, a query tool with syntax
highlighting, and a results grid. Confirmed the winget package id first
before handing him the install command, per protocol (software installs are
his to run, not mine).

**Wrote `Trainings\NestJS\knowledge-base\lessons\04-how-it-all-connects.html`**,
a full lesson, not a log entry. Traces the exact `GET /auth/me` request Anas
already ran in Postman through six stages, real values at each one (the same
`dev-bypass-oid`/`dev@local.test` JSON he actually saw), explains why a
Microsoft-issued badge is used instead of a homegrown login, what a token
actually is, why the guard runs globally, and closes with the same open gap
already flagged in the dev log (tenant resolution), so the lesson and the
log agree with each other instead of contradicting.

Built the diagram with a Python script
(`scratchpad\request_trace_diagram.py`), a 6-stage vertical trace with a
dashed, greyed-out branch for the real-token path that isn't active yet.
Ran an overflow/geometry check (0 problems), a dash/entity sweep (0 hits),
and a sentence-length sweep (fixed 5 sentences over 165 characters), then
verified the render in the browser tool section by section: no console
errors, no clipped text, figcaption doesn't collide with the diagram above
it. Added it to `00-INDEX.md`'s lessons table.

## Same day, fifth round: the log itself went stale

Anas asked directly: "why is the log file not updated?" He was right. This
raw file got updated for both round 3 (the status-table correction) and
round 4 (lesson 04, pgAdmin), but `for-anas/progress-log.html` only got
updated after round 2. Two rounds of real work happened and the page Anas
actually reads never caught up, exactly the failure `PROTOCOL.md` section 2
step 5 exists to prevent.

**Root cause:** updating `raw/` felt like "logging it," so the parallel
`for-anas/` update got skipped, twice in a row, in the middle of doing the
next thing. Fixed by adding the two missing timeline entries and updating
the "last updated" line. Also worth naming: **the two are not
interchangeable.** `raw/` being current does not mean the log is current,
because Anas never reads `raw/`. Only `for-anas/` being current counts.

## Same day, sixth round: the fix from round 5 was itself incomplete

Immediately after round 5, Anas pointed at the "Your first hands-on skill"
section and was, rightly, furious: it still walked through raw terminal
`psql`, the exact thing he'd already asked to replace with a GUI two rounds
earlier, and the timeline entry directly above it literally said "pointed
him at pgAdmin instead of typing raw SQL in a terminal." The page
contradicted itself in the same document.

**Root cause:** round 4 updated the recommendation (pgAdmin) in chat and in
the timeline text, but never went back and rewrote the actual hands-on
walkthrough that Anas would follow. Two different claims about the same
thing, in the same file, never reconciled against each other.

**Fixed:** rewrote the whole "Your first hands-on skill" section to be
pgAdmin end to end (register the server, browse tables with no typing,
Query Tool for the INSERT/SELECT/DELETE round trip), matching what the
timeline entry already claimed. Grepped the file afterward for `psql` to
confirm zero leftover references, not just the one paragraph that got
complained about.

**The actual lesson here, for real this time:** a fix that only touches
the paragraph someone points at is not a fix. Before saying anything is
"corrected" on this page, grep the whole file for the specific
tool/command/claim that changed, not just the section that prompted the
complaint. Written into `PROTOCOL.md` section 8 as a hard requirement, not
a suggestion.

## Same day, seventh round: he actually did it

Anas sent a screenshot of pgAdmin 4, connected to `mmm_dev/mmm_dev@MMM
Local`, Query Tool open with the exact three statements from the
walkthrough (`INSERT ... RETURNING *`, `SELECT * FROM tenants`, `DELETE
FROM tenants WHERE name = 'Anas Test Tenant'`), Data Output showing the
`tenants` table's real column structure. Said "Done!"

Flipped both "Local database" and "Tenants & users tables" from
`Not checked by you` to `Done`, with `why` text describing what he actually
did, not what Claude did. Updated the closing line of the walkthrough
section to record that he completed it, rather than still asking him to.
Added a matching timeline entry. This is the first time every row on this
page that *can* be done today is actually marked done, honestly.

## Same day, eighth round: a real correction, not a mistake this time

Anas mentioned in passing: no Azure subscription yet, but the company
already has Microsoft work accounts at `convergentbt.com`.

This matters more than it sounds. Every earlier "Auth real login" blocker
note on this page, in lesson 04, and in the Azure folder's
`project-context.md` said real Entra login was blocked on "the company
Azure account," conflating two different things: an **Entra ID tenant**
(identity, free, already exists here since work accounts exist under it)
and an **Azure subscription** (paid hosting, genuinely does not exist yet).
Registering an app in Entra ID needs the tenant and an admin role in it,
not a subscription. So the real blocker for Auth's real-login testing may
never have been the subscription at all, it's whether someone has, or can
get, the right role in the tenant that already exists.

**Corrected, not because Claude got it wrong on its own this time, but
because new information came in:**
- `Trainings\Master Microsoft Azure Fundamentals AZ-900 Exam Prep 2026\
  knowledge-base\project-context.md`: current-state table split into
  "tenant exists" vs. "subscription doesn't," open question 1 updated with
  the resolution and the Auth consequence.
- `Trainings\NestJS\knowledge-base\lessons\04-how-it-all-connects.html`:
  the dev-bypass callout no longer says "no company Azure account," says
  the real thing (needs someone with the right admin role in Entra ID).
- This page: the top "if asked" answer, the Auth status row, the "Running
  on Azure" row (now correctly a *different* blocker than Auth), and the
  blockers list, all split into the two separate things.

**Still don't know:** whether Anas himself has Application Administrator or
Global Administrator rights in the `convergentbt.com` Entra tenant, or
needs to ask someone. Asked him directly rather than assuming either way.

## Same day, ninth round: registration actually started

Resolved, but not the way first assumed: **Farhan has the right role, not
Anas.** Anas asked Farhan to create the app registration; Farhan did it and
shared the Client ID (`ceb9994d-7d9d-46b0-be69-19fc48a73251`) and Tenant ID
(`d5619769-1863-41fe-86e3-95000d84f2a6`) back. Wrongly wrote this up
everywhere as "Anas registered the app himself" in the first pass, Anas
corrected it directly ("not I did, okay?"). Fixed in `raw` (this entry) and
in `for-anas/progress-log.html` (top answer, status row, blockers list,
timeline), grepped for every instance rather than the one line pointed at.

**Practical consequence for later:** anything needing Entra tenant-admin
access routes through Farhan, not Anas. Don't assume Anas can self-serve
this again.

## Same day, tenth round: parked, moved to CMP-42

Anas: everything on our end for real Entra login is done, park it until
Farhan clears the portal snag, keep working. Updated `for-anas/
progress-log.html` (last-updated line, blockers list, timeline) to say so
explicitly rather than leaving it reading as an open in-progress item with
no owner. `CMP-40` stays "In Progress" in Jira, that's still accurate, work
is real and ongoing, just blocked on someone else's action.

Moving to `CMP-42` next, per the planned sequence: multi-tenant support,
RLS enforcement, first-login provisioning logic, plus the Epic 6 to Epic 1
move.

## Same day, eleventh round: actual root cause found, on a sandbox copy

Anas built a throwaway second app registration (`fa733969-d53d-46ed-...`,
named "MMM" too, easy to confuse with the real one at first glance, flagged
the mismatch to him before assuming it was the same app) specifically to
find the exact click path without touching the real one Farhan owns.

**Real root cause, not the earlier guesses:** "Add a scope" was silently
failing to save because two *required* fields, Admin consent display name
and Admin consent description, were left blank. Not a Disabled-state issue,
not a portal refresh lag, not a wrong-tab search problem, those were all
reasonable guesses that turned out wrong once actual evidence came in (the
app was missing entirely from "My APIs," not just the one scope). Filled
both fields, scope saved immediately, confirmed showing up correctly.

Verified this is genuinely fixed by having Anas check "My APIs" → search
`access_as_user` on the sandbox app after the fix, not just trusting the
scopes table looked right.

Gave Anas an exact, corrected message to relay to Farhan for the real app
(Client ID `ceb9994d-7d9d-46b0-be69-19fc48a73251`): fill in those two
fields, Enabled, Add scope. Anas is handling that handoff himself, parking
it here, moving to `CMP-42`.

Along the way: Farhan pushed back on needing `access_as_user`, pointing at
an unrelated app of his own ("Nexus," for internal office processes) that
only uses `openid`/`profile`. Explained the ID-token-vs-access-token
distinction and the audience check our guard does; also flagged the honest
alternative (validate the ID token instead, simpler, but not the pattern a
multi-tenant SaaS product should build on) rather than just insisting.
Turned out to be a non-issue: Nexus is unrelated, our own API still needs
`access_as_user`.

Then hit a real Entra portal issue: the `access_as_user` scope doesn't show
up under API permissions → Add a permission → My APIs → search, "No
results." Gave three likely causes to check before assuming anything
structural is wrong (Enabled/Disabled state on the scope, wrong app
selected, portal refresh lag), in that order, cheapest and most likely
first.

**Updated both `for-anas/progress-log.html` and lesson 04** to stop saying
"nobody has registered the app" or "that step hasn't happened yet," since
it has: badge changed from `Blocked` to `In progress`, the blockers list
item rewritten to describe the actual current snag instead of the
already-resolved permissions question, and a new timeline entry added.
Grepped both files for stale phrasing before calling it done, not just the
one paragraph Anas pointed at.
