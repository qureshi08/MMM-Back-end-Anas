# 2026-08-17

## Farhan's ask: figure out how Hammad's engine fits together

Read all four of Hammad's handover docs in full plus his real source code
(`Model Integration Original\`), not just the summary he gave verbally. Built
`dev-log/for-anas/hammad-integration-plan.html` after several iterations —
first draft over-engineered (proposed Dockerizing his whole stack + patching
5 files), corrected down to the actual easiest path: reuse his existing
Colab/ngrok bridge, one small code change (`fetch_data.py` accepts a URL, not
just a local path, since our dataset lives in R2). No Azure work now, dev
only, matches the standing "dev now, Azure later" principle already used for
Postgres. Final format: a literal turn-by-turn "you say this / he does this"
playbook, not an architecture description — that's what actually landed
after three failed attempts at prose/diagrams/roadmaps.

## Amna's five flagged frontend gaps, checked against Hammad's real contract first

1. Row-level data — safe to build, doesn't touch what Hammad receives.
2. Per-channel calibration — **would have actively misaligned**. His real
   contract (`calibrate_and_hyperparameterize.py`) only ever takes one
   overall `{contribution_belief_percent, confidence_percent}` pair, never
   per-channel. Told Amna to shrink the Calibrate wizard instead of building
   per-channel storage.
3. Automatic hyperparameter tuning — real tuning is literally what his
   engine does internally. Told Amna to label the frontend's random-draw
   result "Estimated" rather than build a fake backend equivalent.
4. Combine columns — no conflict with his contract, just unbuilt.
5. Train Model — unchanged, confirmed still deliberately mocked.

## Built, tested, pushed (commit `c630e0a`, then `a237b72`)

- `GET /datasets/:id/rows` — thin wrapper around `parseCsvRows`, already
  used by Assembly and `date-range`.
- `POST /datasets/:id/combine-columns` — sums named columns per real date
  row, paired via `columnMapping.dateColumn`.
- `GET /datasets/:id/date-range` — carried over from an earlier interrupted
  session, was written but never committed; committed now.
- **Real bug found and fixed**: `extractCsvHeaders` (backs
  `GET /datasets/:id/columns`, what Configure's pick-list uses) did a naive
  `split(',')`, while `parseCsvRows`'s `splitCsvLine` (backs the new `/rows`
  endpoint) already handled quoted fields correctly. Any quoted CSV header —
  comma inside or not — would come back under a different key than the row
  data uses, silently zeroing out anything that reads `row[columnName]`.
  Both now share one exported `splitCsvLine`. 51/51 existing tests still
  pass after the change.

Wrote `prompt-for-amna-claude-9.md` (the four items above) and
`prompt-for-amna-claude-10.md` (wire the `date-range` suggestion into
Optimize, plus re-check two old unconfirmed bugs: "Open Model" button and
Edit's exit trap).

## Amna's response, verified live by Anas, real evidence each time

- Row data, correlation table, spend-share bars: real numbers on Optimize,
  confirmed live.
- Combine columns: confirmed via the browser Network tab — real `201`
  response with real summed dates/values, not just a UI change. The
  200/204-preflight confusion ("Failed to load response data") was
  explained: that's the automatic CORS preflight request, which never
  carries a body, not a bug.
- Date-range suggestion: first check showed a mismatch, traced to a stale
  saved value on a *different* dataset, not a bug — a fresh untouched
  dataset showed an exact match (`1/5/2026`–`2/2/2026` in the file, same
  range suggested, with a clear "filled in from your file" banner).
- "View Models" button: confirmed navigating correctly (real URL, real
  Models list loaded).
- Edit's exit trap: confirmed fixed by Anas directly (no screenshot, but
  explicit confirmation after being asked directly whether he'd actually
  tested it, not just assumed).

All five items across both prompts now confirmed by Anas, not just claimed.
`progress-log.html` status table and timeline updated same day.
