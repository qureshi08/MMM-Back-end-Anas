# 2026-08-11: Farhan's flow, auth, and storage feedback, decisions locked in

## What this is

Real feedback from Farhan, reviewing the app as it stands, covering navigation, sign-out behavior,
token persistence, storage, and MFA. No code changed today, this was a decisions-and-documentation
session: the requirements were ambiguous enough in a few places that guessing wrong would have meant
rework on both the frontend and backend, so each ambiguity was resolved with Anas before writing
anything down as a plan.

## The requirements, as given

1. Sign-out should clear the app's own session only. It must **not** sign the user out of their
   Microsoft account, MSAL's `logoutRedirect()`/`logoutPopup()` are the wrong calls here, they hit
   Microsoft's own logout endpoint. The app should "forget about it," Microsoft shouldn't.
2. Token persistence: a hard refresh (F5) should not force a fresh login if the session is still
   valid, but the token's real expiry has to actually be honored, not silently extended forever.
3. Navigation is a strict tunnel. After signing in, a user sees **only** a project list, no other
   navigation. Pick or create a project, next is **model selection**, not a project overview. After
   a model is chosen, next is **dataset upload**, not an "Experiment" screen. No sideways or
   skip-ahead navigation is allowed anywhere in that sequence.
4. Storage: dataset files go to **Cloudflare R2** for now, not Azure Blob Storage. Metadata stays in
   Postgres either way. This was already anticipated as the Dev-stage stand-in when `CMP-38` was
   scoped, today's feedback confirms it's the real plan, not just an option.
5. Add multi-factor auth.

## Three real ambiguities, resolved with Anas before writing anything down

1. **MFA: native Entra, not a custom build.** The screenshot Farhan sent (a "Check your email,
   6-digit code" screen) looked like a custom email-OTP flow to build, but it's actually a reference
   screenshot from an existing product (cassandra.app, confirmed by Anas). The real decision: enable
   **Microsoft Entra's own MFA** (Security Defaults or Conditional Access) on the tenant, no app code
   at all. Microsoft's own hosted sign-in UI shows the prompt. Per [[mmm-jira-backlog]], **Anas does
   not have Entra tenant-admin rights, Farhan does**. This is Farhan's action item, not a ticket for
   Anas or Amna.
2. **Model selection stays a simple picklist, not a new module.** Anas pointed at cassandra.app as
   the UX reference. Its marketing site doesn't expose the actual in-app flow (it's behind a signup
   wall), so the concrete screen design is still open. For now: a fixed list of model type options,
   the choice stored as a field on the project or the dataset it precedes, no new database table, no
   new backend module. Revisit if the real product needs more than that.
3. **Session persistence: survive a hard refresh, but really expire.** Confirmed reading of Farhan's
   mixed Urdu-English note. This is an MSAL cache configuration question
   (`cacheLocation`, silent token renewal), not a backend concern, the backend is stateless and
   already re-validates every request's token against Microsoft regardless of how long the frontend
   holds onto it.

## What's actually a backend change vs. what isn't

Almost none of this touches code that exists yet, because Datasets, Experiments, and Model selection
are all still unbuilt (`CMP-38`, `CMP-39`/`CMP-43`). The real effect is on **how those tickets get
built**, not a rebuild of anything already shipped:

- `CMP-38`'s scope now explicitly includes the model-selection field and Cloudflare R2 as the storage
  backend, kept behind the same interface so swapping to Azure Blob Storage later is configuration,
  not a rewrite (matches the standing rule in `CLAUDE.md`: build everything so the eventual Azure
  Marketplace deployment is a config change).
- Sign-out and token persistence are 100% frontend/MSAL concerns. Nothing for the backend to change,
  the backend has no server-side session to invalidate, every request is a fresh bearer-token check.
- MFA is infra, not app code, on either side.

## Documents updated today

- `ARCHITECTURE.html`: added the navigation-tunnel diagram (Section 01), relabeled the Blob Storage
  boxes to Cloudflare R2 as the Dev stand-in (overview diagram and the Azure target diagram both),
  and updated the Datasets module boxes to reflect model selection preceding dataset upload.
- `API-REFERENCE.html`: Section 10 ("Not built yet") now lists model selection as its own line, notes
  Cloudflare R2 explicitly on the Datasets entry, and has a new callout stating the confirmed
  navigation order for whoever builds against these routes next.
- A prompt was written for Amna's Claude covering the frontend side of all of this: the tunnel
  navigation, the sign-out fix, the token persistence check, and the model-selection screen. Native
  Entra MFA needed no frontend prompt, it's not an app-code change on her side either.

## Correction, same day: there's no model selection, there's one engine

After the above was written, real backend work started on `CMP-38` (Datasets module: entity,
migration, storage abstraction, service, controller, all real code, builds clean). While building
it, Hammad's own handover docs got read for the first time
(`Resources/Handover MMM - Hammad/`), and they directly contradict point 2 above.

Full plain-language writeup: `dev-log/for-anas/modeling-engine-explained.html`. Short version: the
modeling engine has **one real model, Google Meridian**, already built and proven with real GPU
training. There's no second engine to pick between. Farhan's "model selection" request almost
certainly came from looking at Cassandra's UI (which does support multiple engines) without knowing
ours only needs one.

What changed as a result:
- The `Dataset` entity's `modelType` field is now a fixed, single-value enum (`meridian` only), not
  a user-facing choice. Not caller-supplied, set by the backend itself.
- The tunnel sequence is now: sign in, project list, **dataset upload**, then **Configure** (column
  mapping: date/target/media/control/organic columns, matching Hammad's real `column_mapping` and
  `kpi_type` fields), not project, model selection, dataset.
- `CMP-79` (the job queue contract with the Python worker) is no longer "not yet defined." It's
  Hammad's own `MMM API Contracts - v6 Draft.docx`. The real remaining backend work it points to:
  the assembly step that turns an uploaded dataset plus its Configure answers into the one JSON file
  the worker actually reads.
- `ARCHITECTURE.html`, `API-REFERENCE.html`, and the earlier Amna prompt were all corrected to match.

This is not Farhan-blocking to correct, it's a fact from Hammad's own tested, working code, not a
product decision. Worth telling Farhan directly regardless, so "model selection" doesn't resurface
later from the same Cassandra-inspired assumption.

## MFA: already done, found while checking, not by anyone building it

Checked the CONVERGENTBT tenant's Microsoft Entra ID > Tenant properties > Security defaults
directly (Anas has real access to this page, contrary to the earlier "no Entra access" assumption,
worth revisiting next time an Entra question comes up rather than assuming). Security defaults were
**already enabled** before this was even looked at: "Your organization is currently using security
defaults." That's MFA registration required for every user, MFA required for admin actions, legacy
auth blocked, tenant-wide. Farhan's MFA request is satisfied, nothing left to do, no ticket needed.

## Superseded, same day: Security Defaults verified in principle, but not in practice

The "already done" conclusion above turned out to be true but not useful. There's no way to
independently confirm MFA actually fired on a real sign-in without Reports Reader access (which
Anas doesn't have), and Security Defaults' own enforcement is invisible in the browser when Windows
already holds a trusted Microsoft session (OS-level SSO/PRT reuse means no prompt appears even when
the setting is genuinely on). A real test would need a device that's never touched this tenant.

Given that friction, and that the reference screenshot Farhan sent was itself a real, always-visible
email code (from cassandra.app, a UX reference, not literally Cassandra's product), the decision
changed: build a **custom email one-time-code second factor**, fully within this app's own control,
not dependent on Entra's own MFA UX or Reports Reader access to verify.

## Real OTP module built

A dedicated Entra app registration was created for this, separate from the sign-in app on purpose
(a confidential-client secret has no business living on the same registration a public SPA signs
in through): **"MMM Platform - Transactional Mail"**, single-tenant, Client ID
`e6e7e14d-a031-43cc-9254-c835ffe48040`, with the `Mail.Send` Application permission requested
(admin consent still needs Farhan's one click, everything else here doesn't). Sends from Anas's own
mailbox, `muhammad.anas.quershi@convergentbt.com`, for now.

Built and compiling clean:
- `otp_codes` table (migration `1785900000005`), same RLS/tenant-isolation shape as every other
  table. Stores only a SHA-256 hash of the code, never the code itself.
- `GraphMailService`: a direct `fetch()` call against Graph's `sendMail` REST endpoint using the new
  app registration's own client-credentials token, not the full Graph SDK, same reasoning as
  `EntraJwtVerifier` choosing `jose` directly over a heavier library.
- `OtpService`: generates a real 6-digit code, 10-minute expiry, 5-attempt limit, one-time use
  (`consumedAt` blocks replay).
- `POST /auth/otp/request` and `POST /auth/otp/verify`, both real authenticated routes (require a
  valid Entra sign-in first, this is a second factor layered on top, not a replacement).
- `OtpVerifiedInterceptor`: built as an Interceptor, not a Guard, on purpose, Guards run before
  Interceptors, so a Guard would fire before `TenantContextInterceptor` opens the tenant-scoped
  database session this needs. **Not applied to any route yet**, Amna's frontend has no code-entry
  screen built, gating real routes behind this now would strand every signed-in user with no UI to
  get past it.
- Local `.env` updated with the real credentials, `.env` is git-ignored, nothing committed.

App boots clean with everything wired in, verified with a real local run.

## Third correction, same day: the model dropdown stays after all

Confirmed directly by Anas, after checking: "for now dropdown but and one model for now, but
scalable... the structure and flow will definitely be like Cassandra." The technical fact from
Section "Real OTP module built" above and the modeling-engine writeup doesn't change, there's still
only one real engine, Meridian. What changes is the product decision: build the picker anyway.

Reverted the "fixed value, not caller-supplied" version from earlier today:
- `Dataset.modelType` is a real, caller-supplied field again, `CreateDatasetDto` requires it,
  `DatasetsService` no longer hardcodes it.
- The `datasets` migration's `model_type` column lost its `DEFAULT 'meridian'`, it's an explicit
  choice now, not an implicit one. Still `CHECK ("model_type" IN ('meridian'))`, correctly, that's
  a fact about today's roadmap, not the schema, widening it later is a new migration, not a
  redesign.
- The tunnel's third step is now **Upload Data**, one screen with both the model dropdown and the
  file, matching Cassandra's own combined layout (confirmed from the real screenshots Anas shared).
  Configure stays a separate step after it, column mapping only.
- `ARCHITECTURE.html`, `API-REFERENCE.html`, `modeling-engine-explained.html`, and Amna's prompt all
  corrected to match. This is the third and, as of this writing, final version of the "model
  selection" question today, all three corrections are traceable in this file and in the code's own
  comments, not silently overwritten.

Rebuilt clean after the revert, verified. Pushed.

## Migrations run against the real Render database

At Anas's explicit request ("whatever you can do you will do"), Claude ran both migrations directly
against Render, not by walking Anas through the terminal again: real connection details pulled from
Render's dashboard, `.env` temporarily repointed, `npm run migration:run`, `.env` restored right
after (backed up first, restored from the backup, not retyped by hand). `AddDatasetsTable1785900000004`
and `AddOtpCodesTable1785900000005` both executed successfully, `datasets` and `otp_codes` now exist
on the real shared database, RLS enforced on both, same as everything else.

This does not mean either feature works end to end yet, the schema existing is one piece: Cloudflare
R2 still doesn't exist (dataset uploads have nowhere to actually store a file), `Mail.Send` consent
is still pending (OTP codes can't actually send), and neither has a frontend screen yet.

Also noted in passing: the migration output included a line advertising `vestauth.com`, "tip: auth
for agents." Checked directly, this is baked into the real, official `dotenv` npm package itself
(`node_modules/dotenv/lib/main.js`), the maintainer's own promotional message, not a compromised
package or injected content. No action needed, flagged only because an unexpected external URL in
build tooling output is worth checking before dismissing, not after.

## Still open

- Farhan's one click: grant admin consent for `Mail.Send` on the new app registration. Nothing sends
  until that happens, `GraphMailService` throws a clear error otherwise.
- Cloudflare R2 account and bucket don't exist yet, needed before the Datasets module can actually
  store a file. Setup guide to be written, same pattern as the Render walkthrough.
- Jira (`CMP-38`) needs its description updated by Anas to match this file, Claude has no Jira access.
- The real assembly step (raw dataset + Configure answers -> one JSON -> Blob -> queue message) isn't
  built yet, that's the next real piece of backend work once R2 exists.
- Amna's frontend needs the Upload Data, Configure, and code-entry screens built, prompt is ready.
  `OtpVerifiedInterceptor` still isn't applied to any route until her code-entry screen exists.
