# Copy this file to .env and fill in real values.
# .env is git-ignored — never commit real secrets.
#
# The point of every DB_* and AZURE_AD_* value below is that they are just
# config. The same code runs against local Docker Postgres today and Azure
# Postgres Flexible Server later — only these values change, nothing in src/.

# ── App ──────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# ── Database ─────────────────────────────────────────────────────────────
# Local (docker-compose): host=localhost, ssl=false — see docker-compose.yml
# Azure Postgres Flexible Server: host=<name>.postgres.database.azure.com,
# ssl=true is required by Azure, the connection will be refused without it.
DB_HOST=localhost
DB_PORT=5432
# The app's own restricted role — no DDL rights, and Row-Level Security
# actually applies to it. Never the superuser below.
DB_USERNAME=mmm_app
DB_PASSWORD=change-me-locally
# The Postgres bootstrap superuser — migrations only (see data-source.ts).
# RLS cannot apply to this role no matter what, Postgres exempts superusers
# unconditionally, so the app must never connect as this one.
DB_ADMIN_USERNAME=mmm_dev
DB_ADMIN_PASSWORD=change-me-locally
DB_NAME=mmm_dev
DB_SSL=false

# Never true anywhere but a throwaway local sandbox. Schema changes only ever
# go through a migration (npm run migration:run), even in development — the
# multi-tenant schema and its Row-Level Security policies are too easy to
# desync silently otherwise.
DB_SYNCHRONIZE=false

# Log every SQL statement to the console. Useful locally, noisy in Azure.
DB_LOGGING=true

# ── Microsoft Entra ID (auth) ────────────────────────────────────────────
# From your API's own app registration in Entra ID (App registrations →
# your app → Overview). AZURE_AD_TENANT_ID is either the specific tenant GUID
# for a single-tenant registration, or the literal word "common" if the app
# registration is configured as multi-tenant (accounts in any organization).
AZURE_AD_TENANT_ID=common
# The audience your API expects on incoming tokens — the Application ID URI
# or Client ID from that same app registration's "Expose an API" blade.
AZURE_AD_AUDIENCE=api://REPLACE-WITH-YOUR-APP-ID-URI

# Dev-only escape hatch: when true, every request is treated as an already
# authenticated fake user instead of validating a real Entra token, so you
# can build and test other routes before an app registration exists.
# The app refuses to start with this on if NODE_ENV=production.
AUTH_DEV_BYPASS=true

# ── Cloudflare R2 (dataset storage, CMP-38) ────────────────────────────────
# Optional. The app boots fine without these, but every /datasets route
# throws a clear error if you actually try to use one. Dev-stage stand-in
# for the real Azure Marketplace target, Azure Blob Storage — see
# ARCHITECTURE.html Section 06. Get these from the Cloudflare dashboard:
# R2 > Manage R2 API Tokens > Create API Token.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=

# ── Microsoft Graph (real mail: OTP, member invites, training notifications) ──
# Optional, same as R2 above. A separate app registration from the sign-in
# one, single-tenant, with the Mail.Send Application permission (needs
# admin consent) and its own client secret. See
# dev-log/for-anas for the setup this was created against. Originally built
# for the OTP second factor, now shared by every real email the app sends —
# see src/common/mail/.
GRAPH_MAIL_TENANT_ID=
GRAPH_MAIL_CLIENT_ID=
GRAPH_MAIL_CLIENT_SECRET=
GRAPH_MAIL_SENDER=

# Optional. Base URL member-invite and training-notification emails link back
# to (e.g. https://mmm-frontend-amna.vercel.app). Without it those emails
# still send, just with no clickable link.
FRONTEND_URL=

# ── Real modeling engines, one per dataset's chosen ModelType ──────────────
# Required for that engine's datasets to train — since 2026-08-24 there is no
# mock fallback, Train Model throws a real error if the right one isn't set.
# Each is the current ngrok address for that engine's own Colab notebook, no
# trailing slash (e.g. https://xxxx.ngrok-free.dev). Changes every time that
# Colab runtime restarts, update it here when it does.
MODEL_ENGINE_URL=
# PyMC-Marketing, Hammad's second real engine (2026-08-27) — a separate Colab
# notebook, so a separate ngrok URL. Same /train, /status, /results contract
# as Meridian above, just routed to a different host based on the dataset's
# modelType. Real, meaningful difference: PyMC's /train is synchronous (the
# request doesn't return until the whole run finishes), handled in
# DatasetsService.train — nothing extra to configure for that here.
MODEL_ENGINE_URL_PYMC=
# The word you and Hammad agreed on, sent as the X-Internal-Secret header on
# every call, to both engines above. Without this set, anyone who has either
# ngrok URL could call /train, /status, /results directly — always set this
# once either MODEL_ENGINE_URL is set. Must match ENGINE_SHARED_SECRET in
# each Colab notebook's api.py cell.
MODEL_ENGINE_SECRET=
