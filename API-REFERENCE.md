# Backend API Reference

**The sole API reference is [`API-REFERENCE.html`](./API-REFERENCE.html), open it in a browser.**

This `.md` file exists only because GitHub renders it inline when browsing the repo, so anyone
landing here sees this pointer instead of nothing. It is not kept in sync with the HTML version and
should not be read as current. Every real detail, base URL, authentication, how to get a real token,
every endpoint, the data model, and what is not built yet, lives in `API-REFERENCE.html`.
